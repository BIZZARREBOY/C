#include <stdio.h>  //Biblioteca stándar de entrada y salida

int main(){ //Funcion main, que es lo que va a ejecutar nuestro programa
    printf("Hola prueba\n"); // Imprime en consola la cadena que le ingreses, para indicar que es una cadena usamos las comillas 
                            //dobles o tambien podemos usar simples
    return 0; //Regresa un 0
}

/* Un programa se compone de funciones y variables 

    Funciones -> Argumentos (valor o por referencia), ejecutan las operaciones que le indiquemos, puede ir vacia. 
                Una función se le indica en que se abre o cierra  con "{}""

                
    Variables -> Espacio en la memoria que les asignas un tipo de dato (int, float, char, double, long, short), almacenan valores 
                que usamos en el código o en los cálculos.

*/